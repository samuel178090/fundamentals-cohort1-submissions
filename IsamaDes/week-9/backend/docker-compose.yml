version: "3.9"

services:
  app:
    build: .
    container_name: legacybridge-app
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: legacybridge-redis
    ports:
      - "6379:6379"
