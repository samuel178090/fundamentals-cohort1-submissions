import React from "react";

export const About: React.FC = () => (
  <main className="p-4">
    <h2 className="text-xl font-bold mb-2">About</h2>
    <p>
      This frontend is built with React + TypeScript + Vite and consumes the
      backend API.
    </p>
  </main>
);
